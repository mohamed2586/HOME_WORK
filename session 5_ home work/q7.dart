void main() {
  /*
 Create a program with the scores [10, 0, 20, 30]. Ignore the zeros, add the other numbers together,
 and print the total
  */
  List<int> scores = [10, 0, 20, 30];
  for (var items in scores) {
    if (items == 0) {
      continue; // انا مش عارفها وشوفتها في شات جي ب تي
    }
    print(items);
  }
}
